<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" android:padding="@dimen/shape_padding">

    <solid android:color="@color/light_grey"/>
    <corners android:radius="@dimen/corners_round_radius" />
    <stroke
        android:width="@dimen/stroke_width"
        android:color="@color/dark_grey"
        />
    <padding
        android:top="@dimen/search_padding"
        android:bottom="@dimen/search_padding"
        android:left="@dimen/search_padding"
        android:right="@dimen/search_padding" />
</shape>